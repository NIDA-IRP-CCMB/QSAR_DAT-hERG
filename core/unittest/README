## This README describes steps to run the unittests in the core

## Unittest for filters

# use these following commands

module load python
cd ~/repositories/herg/core/
python -m unittest -v unittest_filters.py

## Unittest for buildmodel

# use these following commands

module load python
cd ~/repositories/herg/core/
python -m unittest -v unittest_buildmodel.py


